# Points the frontend at the backend. Defaults to http://localhost:8000
# if unset, so this file is optional for local dev — copy it to .env.local
# only if you're running the backend somewhere else.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
