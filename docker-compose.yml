services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: venv_user
      POSTGRES_PASSWORD: venv_pass
      POSTGRES_DB: venv_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
