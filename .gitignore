# --- Python (backend) ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.db
.pytest_cache/
backend/uploads/
# e2e_real_llm.py --save-report output (e.g. run1.json) — its own --help
# suggests that exact filename, which made it easy to accidentally `git add`
# once alongside real changes. Local debugging output, not source.
backend/run*.json

# --- Node (frontend, once it exists) ---
node_modules/
dist/
build/
.next/

# --- Secrets ---
.env
.env.local

# --- Editor / OS ---
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
